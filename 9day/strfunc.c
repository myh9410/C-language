/*
 * strfunc.c
 *
 *  Created on: 2017. 2. 20.
 *      Author: Administrator
 */
#include <stdio.h>
#include <string.h>
/*int main(){
	char sname[10]="honggil";
	char temp[10];

	strcat(sname,"dong");
	printf("strcat결과 : %s\n",sname);

	if (strcmp(sname,"honggildong")==0)
		printf("동일한값입니다.\n");
	//strcmp는 2개의 문자열이 같으면 0
	strcpy(temp,sname);
	printf("strcpy결과 : %s\n", temp);

	int k;
	k=strlen(sname);
	printf("strlen 결과 : %d\n",k);

	printf("strchr 결과 : %s\n",strchr(sname,'g'));

	return 0;
}
*/

